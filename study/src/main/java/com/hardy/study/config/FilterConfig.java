package com.hardy.study.config;

import org.springframework.boot.web.servlet.FilterRegistrationBean;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class FilterConfig {
//
//    @Bean
//    public FilterRegistrationBean<filter> filter1(){
//        FilterRegistrationBean<filter> bean = new FilterRegistrationBean<>(new filter);
//        //원하는 url패턴에 필터를 적용시킬 수 있음
//        bean.addUrlPatterns("/*");
//        //filter의 실행 순서를 결정, 숫자가 낮을수록 먼저 실행
//        //지정하지 않을 시 무작위실행
//        bean.setOrder(0);
//        return bean;
//    }

}
